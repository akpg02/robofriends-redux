# RoboFriends - React + Vite + Redux

This implementation was created React, Redux, Hooks using functional components.

To run the React project:

1. Clone this repo
2. Run npm install
3. Run npm dev
